import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mejora.cit',
  appName: 'mejora-cit',
  webDir: 'src',
  server: {
    androidScheme: 'https'
  }
};

export default config;
